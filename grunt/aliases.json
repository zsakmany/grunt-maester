{
  "default": [],
  "build": [
    "eslint:dist",
    "clean:all",
    "browserify:dist",
    "karma:dist",
    "uglify:dist",
    "less:dist"
  ],
  "devBuild": [
    "eslint:dev",
    "browserify:devBuild",
    "less:dist"
  ],
  "coverage": [
    "clean:coverage",
    "clean:test",
    "browserify:coverage",
    "karma:coverage"
  ]
}
